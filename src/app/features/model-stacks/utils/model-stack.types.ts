// Canonical shapes live in `domain/model-stacks/`. Kept here as a re-export so existing
// service / container imports stay valid through the migration.
export type {
  ModelStack,
  CreateModelStackPayload,
  UpdateModelStackPayload,
} from '@domain/model-stacks';
